<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Green Cycling Hub | Eco-Friendly MTB, Road & E-Bike Reviews</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* All your CSS styles here - keep the complete CSS from earlier */
        :root {
            --primary: #27ae60;
            --secondary: #2c5530;
            --accent: #8fbc8f;
            --e-bike: #4caf50;
            --light: #f1f8e9;
            --dark: #1b5e20;
            --gray: #78909c;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        /* ... include ALL your CSS styles here ... */
        
    </style>
</head>
<body>
    <!-- Your complete HTML structure here -->
    <header>
        <!-- Header content -->
    </header>

    <section class="hero">
        <!-- Hero content -->
    </section>

    <!-- All other sections -->
    
    <footer>
        <!-- Footer content -->
    </footer>

    <script>
        // Your JavaScript code here
        document.addEventListener('DOMContentLoaded', function() {
            // Tab functionality
            const guideTabs = document.querySelectorAll('.guide-tab');
            const guideContents = document.querySelectorAll('.guide-content');
            
            guideTabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    guideTabs.forEach(t => t.classList.remove('active'));
                    guideContents.forEach(c => c.classList.remove('active'));
                    
                    tab.classList.add('active');
                    const tabId = tab.getAttribute('data-tab');
                    const contentId = `${tabId}-guides`;
                    const contentElement = document.getElementById(contentId);
                    if (contentElement) {
                        contentElement.classList.add('active');
                    }
                });
            });
            
            // Mobile menu functionality
            const mobileMenu = document.querySelector('.mobile-menu');
            const nav = document.querySelector('nav ul');
            
            if (mobileMenu && nav) {
                mobileMenu.addEventListener('click', () => {
                    nav.style.display = nav.style.display === 'flex' ? 'none' : 'flex';
                });
            }
            
            // Responsive adjustments
            window.addEventListener('resize', () => {
                if (window.innerWidth > 768 && nav) {
                    nav.style.display = 'flex';
                } else if (nav) {
                    nav.style.display = 'none';
                }
            });
        });
    </script>
</body>
</html>
